import Header from '../../components/Header';
import ListCards from '../../components/ListCards';

const Home = () => (
    <>
        <Header type="home" />
        <ListCards type="home" />
    </>
);

export default Home;
